let myFun =()=>{
    let radius = 2
    // const PI = 3.14
    // let area = PI * radius * radius
    // alert("Area of radius" + radius + " circle is: " + area)

    let area = Math.PI * radius * radius
    alert(`Area of radius ${radius}  circle is:  ${area}`) //template literal ES6
}